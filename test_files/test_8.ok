fun dummy(i32 a, u32 b): bool {
    ret true;
}

fun main(): u64 {
    i32 a = -1;
    u32 b = 12;
    u32 c = 10;
    bool d = true;
    i64 e = -100;
    u64 f = 100;

    e = e + 200;

    # u32 b = -12;   # type error

    # b = -10;       # type error

    # a = b - 1;     # type error (a is i32 and b-1 is u32)

    # if a < b {     # cannot compare i32 and u32
    # } else {}

    # if b > c {       # unsigned int comparison
    #     ret b;
    # } else {
    #     ret c;
    # }

    dummy(-2, 1);
    # dummy(2, kaixo);

    ret f + 10;
}
