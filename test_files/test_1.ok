# Testing basic operations: 
#   - assignation
#   - binary and unary expressions 
#   - operator precedance
#   - variable declaration 

#  Simple function 
fun add(i32 x, i32 y): i32 { ret x + y; }

fun dummy(i32 a) { a = a + 1; }

fun main(): i32 {
    # init variables
    # bool a = !(true || true);
    i32 b  = 3;
    i32 c  = 1;

    if c != 2 && c == 1 {
        if c != 1 {
            ret 1;
        } else {
            ret 2;
        }
        # dummy(1);
    } else {
        ret 3;
    }

    # c = 12 + 1 / 2 + 21 * 2; 

    # if true {
    #     ret c;    
    # } else {
    #     ret b;
    # }
    dummy(c);

    # ret add(c, b); # return statement
}
